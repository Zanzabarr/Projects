<?php
// the admin config.php grabs the site's main config.php from the local includes folder
$local_config_path = dirname(__FILE__)."/../../includes/config.php";
include_once($local_config_path);

